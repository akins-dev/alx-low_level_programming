#include "main.h"

/**
 * main - Entry point
 *
 * @argc: argument count
 * @argv: Argument vector (array of pointers to char)
 *
 * Return: Always 0 (success)
 */

int main(int argc, char *argv[])
{i
	char i[] = argv[0];

	printf("%d\n", argc);
	return (0);
}
