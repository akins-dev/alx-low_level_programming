#include "main.h"

/**
 * main - Entry point
 *
 * @argc: argument count
 * @argv: Argument vector (array of pointers to char)
 *
 * Return: Always 0 (success)
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);
	return (0);
}
